[[INTERESTING]]
In order to have custom aliases you need to edit `home/.gitconfig`
```
[alias]
	pipi = add -A
	caca = commit
	prout = push origin main
	proutMaster = push origin master
```

Of course you should do more useful thing out of aliases